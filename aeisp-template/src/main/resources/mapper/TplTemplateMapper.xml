<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aeisp.template.mapper.TplTemplateMapper">

    <select id="selectOnlineList" resultType="com.aeisp.template.entity.TplTemplate">
        SELECT *
        FROM tpl_template
        WHERE status = 1
          AND deleted = 0
        <if test="scenario != null and scenario != ''">
            AND scenario = #{scenario}
        </if>
        ORDER BY sort_weight DESC, created_at DESC
    </select>

    <update id="incrementUsageCount">
        UPDATE tpl_template
        SET usage_count = usage_count + 1
        WHERE id = #{id}
    </update>

    <select id="selectTopHotTemplates" resultType="java.util.Map">
        SELECT id, template_name AS templateName, usage_count AS usageCount
        FROM tpl_template
        WHERE deleted = 0
        ORDER BY usage_count DESC
        LIMIT #{limit}
    </select>

    <select id="selectMaxTemplateCode" resultType="java.lang.String">
        SELECT MAX(template_code)
        FROM tpl_template
        WHERE template_code LIKE CONCAT(#{prefix}, '%')
    </select>

</mapper>
