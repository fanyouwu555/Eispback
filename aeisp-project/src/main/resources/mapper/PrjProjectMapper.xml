<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aeisp.project.mapper.PrjProjectMapper">

    <select id="selectDailyProjectStats" resultType="java.util.Map">
        SELECT DATE(created_at) AS date,
               COUNT(*) AS createdCount
        FROM prj_project
        WHERE deleted = 0
          AND created_at &gt;= #{start}
          AND created_at &lt;= #{end}
        GROUP BY DATE(created_at)
        ORDER BY date ASC
    </select>

    <select id="countBenchmarkProjects" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM prj_project
        WHERE deleted = 0
          AND is_pinned = 1
    </select>

</mapper>