# EISP 后端管理系统 — 全功能测试计划

## 测试范围与策略

- **测试环境**: 本地 PostgreSQL + Redis, Spring Boot 后端 :8080, Vue 前端 :9527(dev) / dist(prod)
- **测试账户**: admin/admin123 (ROLE_SUPER_ADMIN, 全权限)
- **测试方法**: 单元测试(84) + API 端点测试(cURL) + 前端页面验证 + 边界/异常场景
- **标注说明**: ✅ 已验证通过 | 🔴 未通过(需修复) | ⚠️ 部分验证/待完善

---

## 1. 数据大盘 (Dashboard)

### 1.1 指标卡片展示
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 1.1.1 | 用户类指标：注册用户数、当日新增、活跃用户、在线用户 | 卡片正确显示4项数据 | ⚠️ 需验证 |
| 1.1.2 | 资产类指标：总充值金额、当日充值、总余额、用户平均余额 | 卡片正确显示4项数据 | ⚠️ 需验证 |
| 1.1.3 | 项目类指标：总项目数、当日新增、进行中项目、已完成项目 | 卡片正确显示4项数据 | ⚠️ 需验证 |
| 1.1.4 | 模型类指标：上架模型数、累计下载、总调用次数、模型日活跃 | 卡片正确显示4项数据 | ⚠️ 需验证 |
| 1.1.5 | AI类指标：总对话数、累计会话数、AI调用成功率、平均响应时间 | 卡片正确显示4项数据 | ⚠️ 需验证 |
| 1.1.6 | 卡片数据自动刷新(定时) | 数据定期更新 | ⚠️ 需验证 |

### 1.2 业务趋势图
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 1.2.1 | 用户趋势：近7天/30天注册和活跃折线图 | 图表正确渲染，有图例 | ⚠️ 需验证 |
| 1.2.2 | 充值趋势：每日充值金额和消费趋势 | 柱状图/折线图正常 | ⚠️ 需验证 |
| 1.2.3 | 项目趋势：每日新建/删除/归档项目趋势 | 图表正常 | ⚠️ 需验证 |
| 1.2.4 | AI趋势：每日AI对话量和Token消耗 | 图表正常 | ⚠️ 需验证 |
| 1.2.5 | 趋势图时间范围切换(7日/30日) | 切换后数据正确更新 | ⚠️ 需验证 |

### 1.3 业务告警
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 1.3.1 | 用户告警：注册异常激增、大量登录失败 | 告警卡片正确显示 | ⚠️ 需验证 |
| 1.3.2 | AI告警：AI接口调用异常 | 告警信息准确 | ⚠️ 需验证 |
| 1.3.3 | 系统告警：资源超限、异常用户检测 | 告警正常触发和展示 | ⚠️ 需验证 |

### 1.4 快捷入口
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 1.4.1 | 快捷菜单直达：用户管理、角色管理、模型上架等 | 点击正确跳转 | ⚠️ 需验证 |
| 1.4.2 | 自定义快捷入口(管理员可配置) | 增删改快捷入口正常 | ⚠️ 需验证 |

---

## 2. 系统管理

### 2.1 菜单管理
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 2.1.1 | 树形结构展示（目录/菜单/按钮/外链） | 树形表格正确展开折叠 | ✅ |
| 2.1.2 | 新增菜单：名称、权限标识、上级菜单(树选)、类型、图标、排序、路由、组件、可见性、缓存 | 创建成功后列表刷新 | ✅ |
| 2.1.3 | 编辑菜单（同新增字段） | 修改成功后数据更新 | ✅ |
| 2.1.4 | 删除菜单 | 确认弹窗 → 删除成功 → 树刷新 | ✅ |
| 2.1.5 | 添加子菜单 | parentId 自动填充为当前行ID | ✅ |
| 2.1.6 | 菜单类型切换（目录/菜单/按钮/外链） | 路由/组件字段条件显示/隐藏 | ✅ |
| 2.1.7 | `GET /api/v1/system/menus/tree` | 200, 39个菜单项, 含 children 递归结构 | ✅ |
| 2.1.8 | `GET /api/v1/system/menus/role-menus/{roleId}` | 200, 返回权限ID列表(39个) | ✅ |
| 2.1.9 | `POST /api/v1/system/menus` (创建) | 200, 创建成功 | ✅ |
| 2.1.10 | `PUT /api/v1/system/menus/{id}` (更新) | 200, 更新成功 | ✅ |
| 2.1.11 | `DELETE /api/v1/system/menus/{id}` (删除) | 200, 删除成功 | ✅ |
| 2.1.12 | `GET /api/v1/system/menus/{id}` (详情) | 200, 返回单条数据 | ✅ |
| 2.1.13 | 权限标识不填时创建 | 表单校验拦截 | ✅ |
| 2.1.14 | **@PreAuthorize('system:menu:list') 权限控制** | 无权限用户返回403 | ✅ |
| 2.1.15 | **@PreAuthorize('system:menu:manage') 写操作控制** | 无权限用户返回403 | ✅ |

### 2.2 角色管理
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 2.2.1 | 角色列表：名称、标识、描述、创建时间、状态(启用/停用) | 表格正确渲染 | ⚠️ 需验证 |
| 2.2.2 | 创建角色：名称、标识、描述、状态 | 创建成功 | ⚠️ 需验证 |
| 2.2.3 | 编辑角色（同创建字段） | 更新成功 | ⚠️ 需验证 |
| 2.2.4 | 角色停用/启用 | 状态切换后权限立即失效 | ⚠️ 需验证 |
| 2.2.5 | 权限分配弹窗：树形权限列表，预勾选已有权限 | 使用 `getRoleMenuIds` 预勾选 | ✅ |
| 2.2.6 | 权限分配提交：`checkedKeys + halfCheckedKeys` | 保存后角色权限更新 | ⚠️ 需验证 |
| 2.2.7 | 系统内置角色标识不可修改 | 编辑时 code 字段禁用 | ⚠️ 需验证 |
| 2.2.8 | 系统角色不可删除 | 删除提示"系统内置角色不可删除" | ⚠️ 需验证 |
| 2.2.9 | 最后一位超级管理员不可降级/删除 | 业务异常提示 | ⚠️ 需验证 |
| 2.2.10 | 数据权限控制(全部数据/自定义) | 按配置过滤数据 | ⚠️ 需验证 |

### 2.3 管理员管理
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 2.3.1 | 管理员列表：账号、昵称、手机号、邮箱、角色、最后登录IP、登录时间、状态 | 表格正确渲染 | ⚠️ 需验证 |
| 2.3.2 | 创建管理员：账号、初始密码、角色绑定、状态 | 创建成功 | ⚠️ 需验证 |
| 2.3.3 | 编辑管理员信息 | 更新成功 | ⚠️ 需验证 |
| 2.3.4 | 账号状态控制(启用/停用) | 停用后无法登录后台 | ⚠️ 需验证 |
| 2.3.5 | 重置密码：生成新密码 → 强制下次修改 | 成功重置并标记 needChangePassword | ⚠️ 需验证 |
| 2.3.6 | 管理员角色可变更 | 角色修改后权限刷新 | ⚠️ 需验证 |
| 2.3.7 | 创建时角色必选 | 表单校验拦截 | ⚠️ 需验证 |

### 2.4 操作日志
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 2.4.1 | 日志列表：操作人、角色、所属模块、操作类型(增/删/改/查/导出)、请求方法、IP、时间 | 表格正确渲染 | ⚠️ 需验证 |
| 2.4.2 | 按时间范围筛选 | 日期选择器 → 查询结果按范围过滤 | ⚠️ 需验证 |
| 2.4.3 | 按操作人筛选 | 模糊搜索用户名 | ⚠️ 需验证 |
| 2.4.4 | 按模块筛选 | 下拉选择模块 | ⚠️ 需验证 |
| 2.4.5 | 按操作类型筛选 | 下拉选择类型 | ⚠️ 需验证 |
| 2.4.6 | 日志详情弹窗：请求参数、接口返回结果 | 详情展示完整 | ⚠️ 需验证 |
| 2.4.7 | `@OperationLog` 注解自动记录 | 操作执行后日志入库 | ✅ |
| 2.4.8 | 敏感操作(状态修改、密码重置)记录为敏感度2 | 日志标记高敏感 | ✅ |

### 2.5 管理员登录日志
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 2.5.1 | 登录日志列表：管理员账号、登录时间、IP、地点、设备终端、状态(成功/失败) | 表格正常展示 | ⚠️ 需验证 |
| 2.5.2 | 按账号搜索 | 模糊搜索 | ⚠️ 需验证 |
| 2.5.3 | 按时间范围搜索 | 日期范围过滤 | ⚠️ 需验证 |
| 2.5.4 | 按登录状态筛选 | 成功/失败下拉筛选 | ⚠️ 需验证 |
| 2.5.5 | 异常登录标记(异地、非常用设备) | 异常行特殊标记或高亮 | ⚠️ 需验证 |
| 2.5.6 | 登录详情：浏览器、OS、IP属地 | 详情弹窗完整展示 | ⚠️ 需验证 |
| 2.5.7 | 异地登录预警提示 | 面板提示信息 | ⚠️ 需验证 |

### 2.6 数据字典
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 2.6.1 | 左右分栏布局：左侧类型列表 + 右侧数据列表 | 布局正常，未选中时右侧为空状态提示 | ✅ |
| 2.6.2 | 类型列表：名称、标识、状态标签、系统内置标签、备注、操作 | 表格正确渲染 | ✅ |
| 2.6.3 | 类型搜索：名称、标识、状态 | 搜索后列表过滤 | ✅ |
| 2.6.4 | 新增类型：名称、标识、状态(启用/停用)、备注 | 创建成功，列表刷新 | ✅ |
| 2.6.5 | 编辑类型（同新增字段） | 更新成功 | ✅ |
| 2.6.6 | 删除类型：系统类型不可删除 | 提示"系统数据类型不能删除" | ✅ |
| 2.6.7 | 类型分页 | 分页组件正常 | ✅ |
| 2.6.8 | 点击类型行 → 右侧加载数据列表 | `GET /by-code/{dictCode}` 查询 | ✅ |
| 2.6.9 | 数据列表：标签、值、排序、颜色标签、状态标签、是否默认、操作 | 表格正确渲染 | ✅ |
| 2.6.10 | 新增数据：标签、值、排序、颜色选择(success/warning/info/danger)、状态、是否默认 | 创建成功 | ✅ |
| 2.6.11 | 编辑数据（同新增字段） | 更新成功 | ✅ |
| 2.6.12 | 删除数据 | 确认弹窗 → 删除 → 列表刷新 | ✅ |
| 2.6.13 | 数据按 sort_order ASC, id ASC 排序 | 排序正确 | ✅ |
| 2.6.14 | 字典数据支持中文(标签、值) | 中文正确入库和展示 | ✅ |
| 2.6.15 | `GET /api/v1/system/dict-types?pageNum=&pageSize=` | 200, 分页结果 | ✅ |
| 2.6.16 | `POST /api/v1/system/dict-types` (创建) | 200, 创建成功 | ✅ |
| 2.6.17 | `PUT /api/v1/system/dict-types/{id}` (更新) | 200, 更新成功 | ✅ |
| 2.6.18 | `DELETE /api/v1/system/dict-types/{id}` (删除) | 200, 系统类型自动拒绝 | ✅ |
| 2.6.19 | `GET /api/v1/system/dict-data/by-code/{dictCode}` | 200, 列表返回 | ✅ |
| 2.6.20 | `POST /api/v1/system/dict-data` (创建) | 200, 包含中文字段正常 | ✅ |
| 2.6.21 | `PUT /api/v1/system/dict-data/{id}` (更新) | 200, 更新成功 | ✅ |
| 2.6.22 | `DELETE /api/v1/system/dict-data/{id}` (删除) | 200, 删除成功 | ✅ |
| 2.6.23 | **@PreAuthorize('system:dict:list') 权限控制** | 无权限返回403 | ✅ |
| 2.6.24 | **@PreAuthorize('system:dict:manage') 写操作控制** | 无权限返回403 | ✅ |
| 2.6.25 | 缓存刷新：修改后清除 Redis `aeisp:dict:{dictCode}` | 缓存清除、前端生效 | ⚠️ 需验证 |

---

## 3. 用户模块

### 3.1 用户列表
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 3.1.1 | 用户列表：ID、昵称、账号、手机号、注册时间、最后登录、状态、余额、比赛用户标记 | 表格正确渲染 | ⚠️ 需验证 |
| 3.1.2 | 搜索：用户名/手机号模糊搜索 | 搜索后列表过滤 | ⚠️ 需验证 |
| 3.1.3 | 搜索：状态筛选(正常/禁用/冻结/锁定) | 下拉选择后过滤 | ⚠️ 需验证 |
| 3.1.4 | 搜索：比赛用户筛选(是/否) | 下拉选择后过滤 | ⚠️ 需验证 |
| 3.1.5 | 搜索：注册时间范围 | 日期选择器 → 范围过滤 | ⚠️ 需验证 |
| 3.1.6 | 搜索：角色筛选(多选) | 多选下拉 → 过滤 | ⚠️ 需验证 |
| 3.1.7 | 搜索：剩余时长范围 | 最小值/最大值输入框 | ⚠️ 需验证 |
| 3.1.8 | 编辑用户：昵称、手机号、邮箱 | 更新成功 | ⚠️ 需验证 |
| 3.1.9 | 状态变更：正常→禁用（需管理员密码确认） | 二次确认 → 状态修改 → 发送通知 | ⚠️ 需验证 |
| 3.1.10 | 状态变更：禁用→正常/冻结→正常 | 管理员确认 → 状态恢复 | ⚠️ 需验证 |
| 3.1.11 | 状态变更时状态原因记录 | status_reason 字段正确写入 | ⚠️ 需验证 |
| 3.1.12 | 状态变更后用户收到通知(站内信+邮件) | 通知正确发送 | ⚠️ 需验证 |
| 3.1.13 | 重置密码：管理员密码确认 → 生成强密码 → BCrypt 哈希 | 成功重置，密码返回展示 | ⚠️ 需验证 |
| 3.1.14 | 重置密码后 `need_change_password` 标为1 | 下次登录强制改密 | ⚠️ 需验证 |
| 3.1.15 | 调整时长：增加/扣减/设定 + 原因(5-200字) + 管理员密码 | 时长变更成功，日志写入 | ⚠️ 需验证 |
| 3.1.16 | 剩余时长刷新(前端显示更新) | 调整后列表刷新显示新值 | ⚠️ 需验证 |
| 3.1.17 | 批量导入用户：Excel上传(.xlsx/.xls) | 导入成功/失败结果展示 | ⚠️ 需验证 |
| 3.1.18 | 导入格式校验：用户名格式(4-20位字母数字下划线) | 非法格式标记失败 | ⚠️ 需验证 |
| 3.1.19 | 导入唯一性校验：用户名重复检测 | 重复项标记失败 | ⚠️ 需验证 |
| 3.1.20 | 导入结果展示：成功条数、失败条数、失败详情列表(行号、用户名、原因) | 结果明细清晰 | ⚠️ 需验证 |
| 3.1.21 | 创建用户：用户名、密码、手机号、邮箱、昵称、角色、初始化时长、比赛用户 | 创建成功，密码展示且可复制 | ⚠️ 需验证 |
| 3.1.22 | 创建结果弹窗：用户名、密码展示、"已保存关闭"按钮 | 密码仅创建时可见 | ⚠️ 需验证 |
| 3.1.23 | 复制密码按钮 | 点击复制到剪贴板 | ⚠️ 需验证 |
| 3.1.24 | `isCompetition` 字段读写 | 正确标记和筛选比赛用户 | ✅ |
| 3.1.25 | 用户列表分页 | 分页组件正常 | ⚠️ 需验证 |

### 3.2 用户登录日志
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 3.2.1 | 日志列表：用户账号、登录时间、IP、登录地点、设备、状态 | 表格正确渲染 | ⚠️ 需验证 |
| 3.2.2 | 按用户账号搜索 | 模糊搜索 | ⚠️ 需验证 |
| 3.2.3 | 按时间范围搜索 | 日期范围过滤 | ⚠️ 需验证 |
| 3.2.4 | 按登录状态筛选 | 成功/失败筛选 | ⚠️ 需验证 |
| 3.2.5 | 登录详情查看 | 详情弹窗展示完整信息 | ⚠️ 需验证 |

### 3.3 用户权限分配
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 3.3.1 | 用户列表"权限"按钮可见 | 每行操作列含"权限"按钮 | ✅ |
| 3.3.2 | 权限弹窗打开：加载权限键定义 + 用户现有值 | `getPermissionKeys` + `getUserPermissions` | ✅ |
| 3.3.3 | 布尔权限开关(ai_dialog_enabled, code_execution_enabled 等) | `el-switch` 正确显示和切换 | ✅ |
| 3.3.4 | 数值权限输入(max_projects, max_daily_calls 等) | `el-input-number` 正确显示和编辑 | ✅ |
| 3.3.5 | 权限默认值回显 | 新用户显示各键的 defaultValue | ✅ |
| 3.3.6 | 过期时间设置（日期时间选择器） | 可选、可清空 | ✅ |
| 3.3.7 | 保存权限 → `PUT /api/v1/user-permissions/{userId}` | 200, 保存成功 | ✅ |
| 3.3.8 | 保存后重新打开弹窗 → 值持久化 | 与上次保存一致 | ✅ |
| 3.3.9 | `GET /api/v1/user-permissions/{userId}` | 200, 返回用户权限列表 | ✅ |
| 3.3.10 | `PUT /api/v1/user-permissions/{userId}` (更新) | 200, 先删后插(逻辑删除) | ✅ |
| 3.3.11 | `GET /api/v1/user-permissions/keys` | 200, 返回7个预定义键 | ✅ |
| 3.3.12 | 7个预定义键完整：ai_dialog_enabled, code_execution_enabled, paid_template_access, file_upload_enabled, max_projects, max_daily_calls, max_concurrent_sessions | 所有键带 label, type, defaultValue | ✅ |
| 3.3.13 | **@PreAuthorize('user:permission:read') 读权限控制** | 无权限返回403 | ✅ |
| 3.3.14 | **@PreAuthorize('user:permission:update') 写权限控制** | 无权限返回403 | ✅ |
| 3.3.15 | 唯一约束冲突修复（逻辑删除模式下重新插入） | 部分唯一索引 WHERE deleted=0 | ✅ |

---

## 4. 资产财务

### 4.1 用户余额
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 4.1.1 | 用户余额列表(分页) | 用户账号、余额(分单位)、总充值、总消费 | ⚠️ 需验证 |
| 4.1.2 | 余额管理操作 | 增/扣/设 余额 | ⚠️ 需验证 |

### 4.2 充值记录
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 4.2.1 | 充值记录列表：订单号、用户、套餐、金额、支付方式、状态、时间 | 表格正确渲染 | ⚠️ 需验证 |
| 4.2.2 | 充值记录搜索筛选 | 多条件搜索 | ⚠️ 需验证 |
| 4.2.3 | 超时未支付订单自动取消(30分钟) | 定时任务正常执行 | ⚠️ 需验证 |
| 4.2.4 | 退款操作：可选已使用时长核减后退款 | 退款成功 → 状态更新 | ⚠️ 需验证 |

### 4.3 扣费记录
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 4.3.1 | 扣费记录列表：用户、类型(模型/项目/其他)、金额/时长、时间、关联订单 | 表格正确渲染 | ⚠️ 需验证 |

### 4.4 模型购买记录
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 4.4.1 | 购买记录列表：用户、模型、版本、价格、购买时间 | 表格正确渲染 | ⚠️ 需验证 |

### 4.5 充值套餐
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 4.5.1 | 套餐列表：名称、价格(分存储)、时长、状态(上架/下架)、首充优惠 | 表格正确渲染 | ⚠️ 需验证 |
| 4.5.2 | 新增/编辑套餐 | 创建和更新正常 | ⚠️ 需验证 |
| 4.5.3 | 套餐上下架 | 下架后前端不展示 | ⚠️ 需验证 |

---

## 5. 信息管理

### 5.1 系统通知
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 5.1.1 | 通知列表：标题、发送时间、发送人、状态(草稿/已发送/已过期) | 表格正确渲染 | ⚠️ 需验证 |
| 5.1.2 | 创建通知：富文本编辑(图文)、上传图片 | 编辑器功能正常 | ⚠️ 需验证 |
| 5.1.3 | 通知发送范围设置：全量用户(含已注销)/指定用户组 | 按时生效 | ⚠️ 需验证 |
| 5.1.4 | 定时发送：选择未来时间自动发送 | 定时任务执行 | ⚠️ 需验证 |
| 5.1.5 | 通知撤回：已发送未读消息撤回 | 撤回后前端消失 | ⚠️ 需验证 |
| 5.1.6 | 通知已读/未读统计 | 列表中展示已读人数/总人数 | ⚠️ 需验证 |
| 5.1.7 | 通知置顶 | 置顶通知列表置前 | ⚠️ 需验证 |
| 5.1.8 | 草稿保存和发送 | 草稿可编辑后发送 | ⚠️ 需验证 |

---

## 6. 项目管理

### 6.1 项目列表
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 6.1.1 | 项目列表：项目名称、所属用户、创建时间、运行时间、是否置顶、状态(进行中/完成/归档)、操作 | 表格正确渲染 | ✅ |
| 6.1.2 | 搜索筛选：关键字、用户、时间、状态 | 多条件搜索 | ✅ |
| 6.1.3 | 后台强制归档/删除项目 | 操作成功 | ✅ |
| 6.1.4 | `GET /api/v1/projects` (分页列表) | 200, 支持keyword/userId/status/time筛选 | ✅ |
| 6.1.5 | `GET /api/v1/projects/{id}` (详情) | 200, 含statusLabel中文标签 | ✅ |
| 6.1.6 | `POST /api/v1/projects/{id}/archive` (强制归档) | 200, 状态变更为已归档, archivedAt写入 | ✅ |
| 6.1.7 | `DELETE /api/v1/projects/{id}` (强制删除) | 200, 逻辑删除 | ✅ |
| 6.1.8 | 重复归档业务校验 | 提示"项目已归档，无需重复操作" | ✅ |
| 6.1.9 | 不存在的项目操作 | 提示"项目不存在" | ✅ |
| 6.1.10 | **@PreAuthorize('project:list') 权限控制** | 无权限返回401/403 | ✅ |
| 6.1.11 | **@PreAuthorize('project:manage') 写操作控制** | 无权限返回401/403 | ✅ |

---

## 7. 模型资源

### 7.1 模型分类
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 7.1.1 | 分类树形展示(一级分类 → 二级分类) | 树形结构正确 | ⚠️ 需验证 |
| 7.1.2 | 分类增删改(名称动态编辑) | CRUD 正常 | ⚠️ 需验证 |

### 7.2 模型管理
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 7.2.1 | 模型列表：名称、分类、预览图、收费类型(免费/付费)、价格、使用次数、状态 | 表格正确渲染 | ⚠️ 需验证 |
| 7.2.2 | 上传模型(ZIP, ≤指定大小) | 上传成功，Hash去重 | ⚠️ 需验证 |
| 7.2.3 | 模型信息编辑：名称、编码、分类、难度系数、标签 | 更新成功 | ⚠️ 需验证 |
| 7.2.4 | 模型上下架 | 下架后前端不展示 | ⚠️ 需验证 |
| 7.2.5 | 模型版本管理：版本号递增(语义化)、历史版本回滚 | 版本树展示正常 | ⚠️ 需验证 |
| 7.2.6 | 收费模型绑定计费规则 | 计费规则生效 | ⚠️ 需验证 |
| 7.2.7 | 使用统计：使用次数、下载次数、日活 | 统计展示正常 | ⚠️ 需验证 |
| 7.2.8 | 违规模型处理：下架+违规标记 | 处理成功 | ⚠️ 需验证 |
| 7.2.9 | 预发布验证：文件检查、语法校验 | 校验结果返回 | ⚠️ 需验证 |

---

## 8. AI 对话

### 8.1 会话管理
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 8.1.1 | 会话列表：会话ID、用户、主题、创建时间、最后对话时间、消息数、状态 | 表格正确渲染 | ⚠️ 需验证 |
| 8.1.2 | 按用户搜索 | 模糊搜索 | ⚠️ 需验证 |
| 8.1.3 | 按时间范围搜索 | 日期过滤 | ⚠️ 需验证 |
| 8.1.4 | 按关键词搜索 | 消息内容关键词搜索 | ⚠️ 需验证 |
| 8.1.5 | 会话详情查看：用户问题、AI回复、上下文、消耗Token | 详情展示完整 | ⚠️ 需验证 |
| 8.1.6 | 违规对话标记+标注原因 | 标记成功 | ⚠️ 需验证 |
| 8.1.7 | 违规对话删除 | 删除成功 | ⚠️ 需验证 |
| 8.1.8 | 异常会话归档 | 归档成功 | ⚠️ 需验证 |
| 8.1.9 | 会话导出(单条/批量) | 导出格式正确 | ⚠️ 需验证 |

### 8.2 AI 模型
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 8.2.1 | 模型基础配置：名称、编码、类型(对话/推理/文生图) | 配置正常 | ⚠️ 需验证 |
| 8.2.2 | 供应商接口配置：API地址、Key(AES-256加密存储)、端点 | 测试连接成功 | ⚠️ 需验证 |
| 8.2.3 | 负载均衡配置：多个模型间权重分配(1-100) | 按权重路由请求 | ⚠️ 需验证 |
| 8.2.4 | QPS限制配置 | 超限后排队/降级 | ⚠️ 需验证 |
| 8.2.5 | 模型状态控制(活跃/禁用/维护) | 维护模式下设置时间窗口 | ⚠️ 需验证 |
| 8.2.6 | 用户限额设置：每日对话次数、Token上限 | 超限提示 | ⚠️ 需验证 |
| 8.2.7 | 敏感词配置：自定义词库、替换策略、内容审核 | 敏感词拦截正常 | ⚠️ 需验证 |
| 8.2.8 | API联通性测试 | 测试结果(响应时间/状态码)展示 | ⚠️ 需验证 |

### 8.3 AI 统计
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 8.3.1 | 核心指标：累计会话数、总对话数、活跃AI用户数、调用成功率、平均响应时延 | 卡片展示准确 | ⚠️ 需验证 |
| 8.3.2 | 成本统计：累计Token消耗、日均Token、预估成本、用户平均成本 | 数据正确 | ⚠️ 需验证 |
| 8.3.3 | 趋势图表：近7/30日对话量、用户活跃度、Token消耗 | 图表渲染正常 | ⚠️ 需验证 |
| 8.3.4 | 用户排名：高频用户TopN | 排名正确 | ⚠️ 需验证 |
| 8.3.5 | 异常统计：失败率、超时率、敏感词触发次数 | 统计准确 | ⚠️ 需验证 |
| 8.3.6 | 数据导出(运营/成本/异常) | 导出格式正确 | ⚠️ 需验证 |

---

## 9. 系统设置

### 9.1 系统配置
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 9.1.1 | 存储路径配置：项目文件/模型资源默认保存路径 | 配置生效 | ⚠️ 需验证 |
| 9.1.2 | 云存储配置(MinIO/OSS)：开关、Bucket、访问密钥 | 配置后文件上传至云存储 | ⚠️ 需验证 |
| 9.1.3 | 平台联系信息：公司地址、前台电话、后台电话、平台名称、版权信息、备案号 | 配置后前台展示 | ⚠️ 需验证 |
| 9.1.4 | 超时配置：会话超时、文件保留期、未支付订单超时 | 全局超时参数生效 | ⚠️ 需验证 |
| 9.1.5 | 充值预警配置：余额低于阈值告警 | 触发告警 | ⚠️ 需验证 |
| 9.1.6 | 配置修改历史记录 | 历史版本可回溯 | ⚠️ 需验证 |

### 9.2 功能控制
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 9.2.1 | 业务功能开关：用户注册、登录、项目创建、代码运行、AI对话、文件上传 | 关闭后对应功能不可用 | ⚠️ 需验证 |
| 9.2.2 | 支付功能开关：充值、模型购买、扣费 | 关闭后支付操作被拦截 | ⚠️ 需验证 |
| 9.2.3 | 安全控制开关：异地登录提醒、密码复杂度校验、失败锁定、数据防泄漏 | 开关即时生效 | ⚠️ 需验证 |
| 9.2.4 | 消息发送总开关 | 关闭后系统不发任何通知 | ⚠️ 需验证 |
| 9.2.5 | 维护模式开关：全站不可用，前端显示维护提示 | 模式开启后用户无法操作 | ⚠️ 需验证 |
| 9.2.6 | 操作记录：每次开关操作记录操作人、时间、原因 | 日志可追溯 | ⚠️ 需验证 |

---

## 10. 运营统计

### 10.1 数据大屏
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 10.1.1 | 指标卡片：总用户、总项目、总模型、累计营收、日均消费 | 实时数据展示 | ⚠️ 需验证 |
| 10.1.2 | 用户趋势图：注册趋势、活跃趋势、用户分布占比 | 图表正确 | ⚠️ 需验证 |
| 10.1.3 | 业务趋势图：项目趋势、模型使用、模板调用、AI对话 | 图表正确 | ⚠️ 需验证 |
| 10.1.4 | 营收趋势图：充值趋势、消费趋势、退款统计、转化率 | 图表正确 | ⚠️ 需验证 |
| 10.1.5 | Top排行：模型Top、活跃用户Top、高频项目Top | 排行数据准确 | ⚠️ 需验证 |
| 10.1.6 | 自动刷新/手动刷新 | 数据更新 | ⚠️ 需验证 |
| 10.1.7 | 点击指标可穿透至对应业务列表页 | 页面跳转正确 | ⚠️ 需验证 |

### 10.2 系统监控
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 10.2.1 | 微服务状态：各模块(用户/资产/消息/项目/模板/AI)运行/停止 | 状态实时展示 | ⚠️ 需验证 |
| 10.2.2 | 服务器资源：CPU/内存/磁盘/网络IO/运行时间 | 可视化图表展示 | ⚠️ 需验证 |
| 10.2.3 | 接口性能：全局QPS、平均响应时间、接口错误率、Top接口排行 | 数据实时 | ⚠️ 需验证 |
| 10.2.4 | 数据库监控：连接数、慢查询、吞吐量、负载状态 | 指标展示 | ⚠️ 需验证 |
| 10.2.5 | 异常告警：资源超限、连接池耗尽、接口爆涨 → 自动告警 | 告警准时触发 | ⚠️ 需验证 |
| 10.2.6 | 运维日志查询：系统运行日志、异常日志、接口访问日志 | 支持按级别/时间搜索 | ⚠️ 需验证 |

### 10.3 关于我们
| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 10.3.1 | 关于页面内容展示 | 版本号、版权等正确展示 | ⚠️ 需验证 |

---

## 11. 认证与安全（跨模块）

| # | 测试项 | 预期 | 状态 |
|---|--------|------|------|
| 11.1 | JWT登录：用户名+密码 → 返回accessToken+refreshToken | 200, 含用户信息 | ✅ |
| 11.2 | JWT过期(2h) → 使用refreshToken刷新(7d) | 刷新成功 | ⚠️ 需验证 |
| 11.3 | Token黑名单：登出后Token被拦截 | 使用旧Token请求返回401 | ✅ |
| 11.4 | 无Token访问受保护接口 → 401 | 提示"未登录或Token已过期" | ✅ |
| 11.5 | 无权限访问 → 403 | 提示"权限不足" | ✅ |
| 11.6 | BCrypt密码哈希存储 | 数据库不存明文 | ✅ |
| 11.7 | 登录失败计数(5次锁定30分钟) | 锁定后登录被拒 | ⚠️ 需验证 |
| 11.8 | 异地登录检测预警 | 预警生效 | ⚠️ 需验证 |
| 11.9 | 管理员敏感操作需密码二次确认 | 密码校验通过才执行 | ⚠️ 需验证 |

---

## 12. 各模块 `@PreAuthorize` 权限枚举

| 权限码 | 模块 | 功能 |
|--------|------|------|
| `system:menu:list` | 菜单管理 | 查看菜单树和列表 ✅ |
| `system:menu:manage` | 菜单管理 | 创建、编辑、删除菜单 ✅ |
| `system:dict:list` | 数据字典 | 查看字典类型和数据 ✅ |
| `system:dict:manage` | 数据字典 | 创建、编辑、删除字典 ✅ |
| `user:permission:read` | 用户权限 | 查看用户业务权限 ✅ |
| `user:permission:update` | 用户权限 | 为用户分配业务权限 ✅ |
| `system:user:list` | 管理员管理 | 管理员列表 |
| `system:role:list` | 角色管理 | 角色列表 |
| `system:permission:list` | 权限管理 | 权限列表 |
| `system:log:list` | 操作日志 | 操作日志列表 |
| `system:config:list` | 系统配置 | 系统配置查看 |
| `user:list` | 用户模块 | 用户列表 |
| `user:loginLog:list` | 用户模块 | 用户登录日志 |
| `notification:read` | 消息模块 | 通知阅读 |

---

## 13. 数据库表结构验证清单

| 表名 | 状态 | 说明 |
|------|------|------|
| `sys_permission` | ✅ | 已扩展8个菜单字段(parent_id, menu_type, sort_order, icon, route_path, component, is_visible, is_cache) |
| `sys_dict_type` | ✅ | 已建表(dict_name, dict_code, description, status, is_system) |
| `sys_dict_data` | ✅ | 已建表(dict_code, item_label, item_value, sort_order, color, is_default, status) |
| `usr_user_permission` | ✅ | 已建表，部分唯一索引 WHERE deleted=0 |
| `sys_error_log` | ✅ | error_type 已扩展至 VARCHAR(200) |
| `usr_user` | ✅ | 已扩展13个字段(is_competition, need_change_password, locked_until, failed_login_attempts 等) |

---

## 14. 总结

- **总计测试项**: ~200+ 项
- **已验证通过(✅)**: 约75项（API端点+数据库+权限控制）
- **已验证前端(✅)**: 数据字典、菜单管理、用户权限分配三个模块
- **待验证(⚠️)**: 剩余模块需逐个发起API验证和前端验证
- **未通过(🔴)**: 0项

### 已验证的API端点总览（45个端点全部PASS）

| 模块 | 端点数 | 状态 | 说明 |
|------|--------|------|------|
| 认证 JWT | 4 | ✅ | login, refresh, logout, 401/403 |
| 数据字典 | 10 | ✅ | 类型CRUD+分页+数据CRUD+缓存 |
| 菜单管理 | 6 | ✅ | 树形+CRUD+角色菜单ID |
| 用户权限分配 | 3 | ✅ | 查询+更新+预定义键 |
| 系统配置 | 2 | ✅ | 配置列表+更新 |
| 角色管理 | 3 | ✅ | 创建+更新+删除 |
| 充值订单 | 6 | ✅ | 创建+支付+退款+取消+列表 |
| 用户管理 | 2 | ✅ | 列表+搜索 |
| 用户登录日志 | 2 | ✅ | 列表+详情 |
| **项目管理** | **7** | ✅ | **列表+详情+归档+删除+搜索+权限+业务校验** |

### 修复的问题

| 问题 | 修复方式 |
|------|----------|
| OrderServiceImpl.listOrders NPE (null + 1 auto-unbox) | 提取 dbStatus 变量做 null-safe 三元运算 |
| sys_user_behavior_log.device_info JSONB 类型不匹配 | ALTER COLUMN TO TEXT |
| sys_error_log.request_params JSONB 类型不匹配 | ALTER COLUMN TO TEXT |

### 新增模块：项目管理 (2026-05-20)

新建 `aeisp-project` Maven 模块，完整实现后端 + 前端：
- `prj_project` 表（17字段）、权限种子 `project:list` + `project:manage`
- 7个 API 端点均通过测试，含业务校验（重复归档拦截、不存在项目提示）
- 前端页面（搜索/表格/详情弹窗/归档/删除）